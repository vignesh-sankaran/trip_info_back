extern crate hyper;

use hyper::client::Client;

#[test]
fn test_new_uuid()
{
    // Connect to API
    let _ = Client::new();
    // Get UUID
    // Access DB
    // Compare if DB record is the same as the received UUID
    assert!(true);
}

#[test]
fn test_add_user_home()
{
    assert!(true);
}

#[test]
fn test_add_user_destination()
{
    assert!(true);
}